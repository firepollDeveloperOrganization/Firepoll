// REMOVE THIS AND COPY THE BOILERPLATE FOR YOUR UNIT TEST

// Just copy this boilerplate for every component
//import React from 'react';
import { expect } from 'chai';
//import { shallow, mount, configure } from 'enzyme';
//import Adapter from 'enzyme-adapter-react-15';

// import the component you want to test
//import someComponent from '../client/src/components/some-component.jsx';

// configure({ adapter: new Adapter() })

describe('some component', function() {
  it('should do something.', function() {
    // let wrapper = shallow(<someComponent repos={[
    //   {name:"foo", url:"chicken"}
    // ]}/>);

  // write the tests below
  expect(true).to.be.true;

  })
  // write more tests 
})

